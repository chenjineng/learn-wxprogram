<!--pages/home/index.wxml-->
<view class="point-list">
  <block wx:for="{{pointList}}" wx:key="index">
    <view class="point" bindtap="navigate" data-path="{{item.path}}">{{item.point_name}}</view>
  </block>
</view>
<tab-bar selected="{{selected}}"></tab-bar>